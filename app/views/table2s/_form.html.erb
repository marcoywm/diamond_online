<%= form_for(@table2) do |f| %>
  <% if @table2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@table2.errors.count, "error") %> prohibited this table2 from being saved:</h2>

      <ul>
      <% @table2.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :product %><br />
    <%= f.text_field :product %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
