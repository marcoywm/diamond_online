<h1>Listing table2s</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Product</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @table2s.each do |table2| %>
  <tr>
    <td><%= table2.name %></td>
    <td><%= table2.product %></td>
    <td><%= link_to 'Show', table2 %></td>
    <td><%= link_to 'Edit', edit_table2_path(table2) %></td>
    <td><%= link_to 'Destroy', table2, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Table2', new_table2_path %>
